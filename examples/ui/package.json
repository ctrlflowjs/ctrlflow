{
  "dependencies": {
    "@ctrlflow/core": "0.0.2",
    "@ctrlflow/ui": "0.0.2",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "parcel": "^2.7.0",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "start": "node ./src/server.js",
    "watch": "nodemon --watch . --watch ../../packages/core/dist",
    "clean": "rimraf dist/*",
    "build:watch": "npm run clean && parcel watch ./src/index.html"
  }
}
