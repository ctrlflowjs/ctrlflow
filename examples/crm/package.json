{
  "dependencies": {
    "@ctrlflow/core": "0.0.2",
    "express": "^4.18.1"
  },
  "scripts": {
    "start": "NODE_OPTIONS=--enable-source-maps node ./src/server.js",
    "watch": "NODE_OPTIONS=--enable-source-maps nodemon --watch . --watch ../../packages/core/dist"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
